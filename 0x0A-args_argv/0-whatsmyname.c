#include <stdio.h>
/**
 * main - Entry point
 * Description:Prints it's own name
 * @argc:Input
 * @argv:input
 * Return:int
 */

int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
